import React from 'react';
import { storiesOf } from '@storybook/react';
import { {{pascalCase name}} } from './{{pascalCase name}}';
import './style'

storiesOf('{{pascalCase name}}', module).add('Demo', () => (	
	<>
		<{{pascalCase name}} />
	</> 
))